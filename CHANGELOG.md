# Changelog

## 0.1.0

- packaged the tool as `github-star-index` with the `star-index` CLI
- added deterministic XML, raw JSON, Markdown summary, and manifest generation
- added refresh reporting in both Markdown and machine-readable JSON
- added agent-ready JSONL bundle outputs for repositories, chunks, and category rollups
- added fixture-driven examples, unit tests, CI, and public release scaffolding

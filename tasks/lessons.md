# Lessons

- Keep the OSS repo fixture-driven; live star caches belong in the operational workspace.
- A single verification wrapper is worth maintaining because CI and local checks need the same artifact contract.
- Bundle outputs need explicit provenance and counts, otherwise downstream agents and automations become brittle.
